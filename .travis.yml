sudo: required

services:
  - docker

before_install:
  - docker build -t sergeysypalo/PlatOps-Docker .

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push sergeysypalo/PlatOps-Docker